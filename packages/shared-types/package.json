{
  "name": "@lms/shared-types",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@lms/config": "workspace:*",
    "typescript": "^5.5.0",
    "rimraf": "^5.0.0"
  }
}

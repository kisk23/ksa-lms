export default function CourseDetailPage({ params }: { params: { id: string } }) {
  return (
    <main>
      <h1>Course Detail: {params.id}</h1>
      {/* TODO: Wire up course detail feature */}
    </main>
  );
}
